apiVersion: apps/v1
kind: Deployment
metadata:
  name: angelpay
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: angelpay
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
