apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: angelpay-dev

resources:
  - ../../base

images:
  - name: ghcr.io/zhima-001/angelpay
    newTag: latest

patchesStrategicMerge:
  - deployment-patch.yaml
