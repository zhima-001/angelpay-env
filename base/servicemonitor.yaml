apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: angelpay
  labels:
    app: angelpay
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: angelpay
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
